pipeline-component:
  prefix: "from-pipeline-component-"

kubernetes-app:
  namespace: ${NAMESPACE}

kafka-app:
  app:
    streams:
      brokers: ${kafka_brokers}
      schemaRegistryUrl: ${schema_registry.url}

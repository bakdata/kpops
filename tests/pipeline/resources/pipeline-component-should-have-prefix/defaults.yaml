pipeline-component:
  prefix: "from-pipeline-component-"

kubernetes-app:
  namespace: ${NAMESPACE}

kafka-connector:
  namespace: ${NAMESPACE}

kafka-app:
  app:
    streams:
      brokers: ${broker}
      schemaRegistryUrl: ${schema_registry_url}

pipeline-component:
  prefix: ""

kubernetes-app:
  namespace: ${NAMESPACE}

kafka-connector:
  namespace: ${NAMESPACE}

kafka-app:
  app:
    streams:
      brokers: ${brokers}
      schemaRegistryUrl: ${schema_registry_url}

streams-app:
  app:
    labels:
      pipeline: ${pipeline_name}
  to:
    topics:
      ${error_topic_name}:
        type: error
        partitions_count: 1
      ${output_topic_name}:
        type: output
        partitions_count: 3

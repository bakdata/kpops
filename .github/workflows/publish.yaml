name: publish
on:
  push:
    tags:
      - '*'

jobs:
  publish:
    uses: bakdata/ci-templates/.github/workflows/python-poetry-publish.yaml@v1.22.0
    with:
      publish-to-test: false
      poetry-version: "1.5.1"
    secrets:
      pypi-token: "${{ secrets.PYPI_TOKEN }}"
